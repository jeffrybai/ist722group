version: 2

models:
  - name: obt_royalty_disbursement
    description: The royalty disbursement fact table combined with all of its outrigger dimensions into one bit table.
     Transaction fact table with one row per a royalty payment 
